{{!
  Write a handlebars template in this file to implement the main tweeter page,
  as described in README.md.
}}
{{> header}}
<main class="twit-container">
   {{#each twits}}
      {{> twit}}
   {{/each}}
</main>
{{#if show}}
   {{> createTwitModal}}
{{/if}}
